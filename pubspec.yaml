name: flutter_apple_camera_workspace
description: Monorepo workspace for Flutter plugins wrapping Apple camera, vision, and capture APIs.
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  melos: ^6.3.0
